# 2026-09-01

## 项目真实完成度审计（面试官视角，含实测）

### 审计方法
- 读取全部源码（03_ChatBot 9 个后端文件 + Vue 前端；04_RAG 4 个 service + schema + 6 个测试）
- 实测运行：RAG 完整链路（retrieve→LLM→citation）、空检索幻觉防护、批量检索质量；ChatBot login/JWT/401/500
- 直接查询 Chroma.sqlite3 确认入库数据

### 关键实测结论
- **RAG 链路可运行**：`怎么修改日报？` → 正确回答 + 引用 chunk 9
- **Empty retrieval 正确**：库外问题 → "知识库中没有找到相关信息"，0 引用（幻觉防护有效）
- **数据陈旧（核心问题）**：库里 18 chunks 是旧版固定 700 字符硬切，其中 chunk 0-7 是目录页/封面垃圾（占一半！）；当前 document_service.py 是章节感知切分（重跑=59 chunks），代码与数据不一致 → 必须重新入库
- **检索召回不稳**：`系统有哪些角色？`（知识库有 1.2 用户角色与权限）→ 无命中；`忘记写日报怎么办？` → 命中 chunk 16（"手册 / — 23 —" 页脚垃圾）
- threshold=0.75 宽松（0.695 放行）；top_k=1 单点脆弱
- **ChatBot 可运行**：login 200 出 JWT、无/伪造 token 401 ✅；Redis 不可达 → /chat 500（健壮性缺口）
- requirements.txt（RAG）为 0 字节；rag_service.py 有 print 调试残留；test_rag_service.py 用 result["metadatas"] 与当前返回不匹配（会 KeyError）
- 无评测代码、无 Docker、无部署、无 README；interview/ 空；docs/ 岗位调研文档优秀（南天电脑等）

### 判定
- 整体路线完成度 ≈ 45%；就业准备度 ≈ 50/100；最接近"实习可投"边缘
- 阶段状态：Python/Backend L3-4、LLM API L3、ChatBot L3-4（90%）、RAG L3 偏下（55%）、Agent L0、工程化 L1、项目包装 L1-2
- 用户已有一份前端实习经历，9 月目标投实习（昆明/成都）
- 下一步优先级：①RAG 清理+重新入库+上传/SSE 闭环 ②30 问评测出数字（Hit Rate）③Agent 最小项目 ④Docker 部署 ⑤README+简历+面试题

### 待办提醒
- .tmp_models 残留 18GB 临时模型文件（可清理，需用户确认）
- 停用 8001 测试端口的 uvicorn 进程

## W1-D1/D2 RAG 数据重做（已完成，含教学）

用户要求：改代码必须配套教学，不能只代做。模式 =「我改 + 讲透原理 + 留作业」。

### 改动
- `document_service.py`：新增 clean_lines（页码正则/页眉自动识别(出现≥8次且长度≥6)/目录点号行/孤立"目录"）；章标题 CHAPTER_PATTERN 不单独成块，作为 chapter_prefix 拼到下一小节；新增 prepare_document()、get_section_title()；extract_text_from_pdf 加 clean=True 参数
- `vector_service.py`：新增 reset_collection()；id 改为 {doc_stem}_chunk_{i} 防多文档冲突；add_documents 加 id_prefix 参数
- `rag_service.py`：DISTANCE_THRESHOLD 0.75→1.0（数据标定：库内 top-1 距离 0.548~0.918，库外 1.155~1.306，隔离带取中）；删 2 处调试 print；warning 日志补 query
- 新增 `backend/ingest.py`：清库→切分→向量化→入库→自检，支持 --dry-run 预览

### 实测结果
- 10450→5627 字符（去噪 46%），18 旧 chunk → 28 干净 chunk，全部带 section metadata
- 8 个库内问题 8/8 命中正确章节（此前 2 个 MISS 且常命中垃圾）
- 库外问题幻觉防护仍有效（2/2 拦截，0 引用）
- 教学文档：docs/W1-D1_D2_RAG数据重做_教学.md（含原理/改动对照/阈值标定法/自查清单/面试话术/作业）

### 已知遗留（下节课作业，故意留给用户动手）
- top_k=1："忘记写日报"答案缺 FAQ 的"3天内补录"信息 → 作业：top_k 改 3 + 思考 precision/recall trade-off
- W1 后续：D3 文档上传接口、D4 SSE 流式、D5 top_k 调优
